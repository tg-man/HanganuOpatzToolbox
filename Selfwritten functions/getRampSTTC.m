function RampSTTC = getRampSTTC(experiment, RampMatrix_1, RampMatrix_2, lags, repeat_calc, save_data, ramp_times, folder2save)
% by Tony; compute STTC between areas during a ramp stimulation 


if ~ exist && repeatCalc == 0 
else
    for lag_idx = 1 : numel(lags) 
        lag  = lags(lag_idx) 

        for ramp = 

            for unit1 

                for unit2 
                    
                    STTC_pre(lag_idx, ) = getSTTC(num_spikes1, num_spikes2, lags(lag_idx), length_rec, spikes1, spikes2);
                    STTC_ramp = 
                end 
            end 


        end 





        

    end 


end %
end % function end 